from flask_http_server import run_app
from threading import Thread

if __name__ == '__main__':
    # flask_thread = Thread(target=run_app)
    # flask_thread.start()
    # flask_thread.join()
    run_app()
